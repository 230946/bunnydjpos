# ─────────────────────────────────────────────────
# BUNNYDJPOS — Variables de entorno para MariaDB/XAMPP
# Copia este archivo como .env
# ─────────────────────────────────────────────────

# Motor: 'mysql' para MariaDB/MySQL (XAMPP) | 'pg' para PostgreSQL
DB_TYPE=mysql

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=bunnydjpos

PORT=3001

JWT_SECRET=bunnydjpos_secreto_2024_cambia_esto
JWT_EXPIRES=8h

SUPERADMIN_EMAIL=superadmin@bunnydjpos.com
SUPERADMIN_PASS=Admin123!

ALLOWED_ORIGIN=*
UPLOADS_DIR=./uploads
